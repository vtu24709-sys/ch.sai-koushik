<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>VEL Tech Logo</title>
  <style>
    :root{
      --bg: #ffffff;
      --page-bg: #f3f4f6;
      --accent: #8b0000;
      --max-size: 360px; /* change to preferred max logo size */
    }

    html,body{
      height:100%;
      margin:0;
      font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial;
      background:var(--page-bg);
      color:#222;
    }

    .container{
      min-height:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
      box-sizing:border-box;
    }

    .card{
      text-align:center;
      background:var(--bg);
      border-radius:12px;
      padding:20px;
      box-shadow:0 6px 20px rgba(15,15,15,0.08);
      max-width:520px;
      width:100%;
    }

    figure {
      margin:0 0 12px 0;
      display:inline-block;
    }

    /* responsive image sizing: scales down on narrow screens */
    img.logo {
      display:block;
      max-width: min(var(--max-size), 80vw);
      height: auto; /* preserves aspect ratio */
      border-radius: 50%; /* optional: keeps circular appearance if image supports it */
      border: 3px solid var(--accent);
      box-sizing: border-box;
    }

    h1{
      font-size:1.05rem;
      margin:8px 0 0 0;
      line-height:1.2;
    }

    p.note{
      margin:8px 0 0 0;
      font-size:0.9rem;
      color:#666;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card" role="region" aria-labelledby="logo-title">
      <figure>
        <!-- Make sure LOGO.png is in the same folder as this HTML file -->
        <img class="logo" src="LOGO.png" alt="VEL Tech logo" />
      </figure>

      <h1 id="logo-title">VEL Tech Rangarajan Dr. Sagunthala R&D Institute</h1>
      <p class="note">If you see a broken image icon, verify the file is named <code>LOGO.png</code> and located beside this HTML file.</p>
    </div>
  </div>
</body>
</html>
